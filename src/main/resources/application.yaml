spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pocker
    username: postgreuser
    password: 5564
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
